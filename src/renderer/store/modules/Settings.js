const state = {
    voip: {
        host: null,
        sip: null,
        sipPass: null,
        event: null
    }
}

const mutations = {
}

const actions = {
}

export default {
    state,
    mutations,
    actions
}
